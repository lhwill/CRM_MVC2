
@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Show</title>
    <script src="~/js/vue.min.js"></script>
    <script src="~/js/axios.min.js"></script>
</head>
<body>
    <div id="app">
        <table class="table table-bordered">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>工号</th>
                    <th>密码</th>
                    <th>姓名</th>
                    <th>身份证</th>
                    <th>部门编号</th>
                    <th>性别</th>
                    <th>手机</th>
                </tr>
            </thead>
            <tbody>
                <tr v-for="item in emps">
                    <td v-for="(value,key) in item">{{value}}</td>
                </tr>
            </tbody>
        </table>
    </div>
    <script type="text/javascript">
        //使用axios实现 Vue.js 2.0 版本推荐使用 axios 来完成 ajax 请求。Axios 是一个基于 Promise 的 HTTP 库，可以用在浏览器和 node.js 中。
        new Vue({
            el: '#app',
            data:{
                emps:''
            },
            mounted() {
                axios
                    .get('http://localhost:8081/api/baseinfo/GetEmp')
                    .then(response => (this.emps = response.data))
                    .catch(function (error) { // 请求失败处理
                        console.log(error);
                    });
            }
        })
    </script>
</body>
</html>